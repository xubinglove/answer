import Vue from 'vue'
import Vuex from 'vuex'
import axios from 'axios'
Vue.use(Vuex)
const state={

}
const mutations={
}
var actions={  
    
}
var getters={
}

export default  new Vuex.Store({
  state,
  mutations,
  actions,
  getters
})
